{
  "name": "@furnistore/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "dotnet watch --project ./API.Furnistore.API/API.Furnistore.API.csproj run",
    "build": "dotnet build API.sln -c Release",
    "clean": "dotnet clean API.sln"
  }
}
